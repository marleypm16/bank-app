export type SignUpParams = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    dateOfBirth: Date;
    zipCode: string;
    state: string;
    address: string;
}